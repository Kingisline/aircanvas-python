## 🖌️ AirCanvas – Virtual Hand-Tracking Drawing App

AirCanvas is a Python + OpenCV project that allows you to draw in the air using your finger, without touching anything.
Using MediaPipe hand tracking, the index finger motion is traced and drawn onto a virtual canvas in real time.

### 🚀 Features

- ✋ Hand detection using MediaPipe

- 🧠 Index fingertip tracking

- 🎨 Draw in the air using gestures

- 🌈 Multiple brush colors (optional)

- 🧽 Gesture-based clear screen

- ⚡ Real-time drawing with webcam

### 📦 Requirements

Install dependencies using the provided `requirements.txt`:

```
opencv-python>=4.5.0
mediapipe>=0.8.9
numpy>=1.19.0
```

Install them with:
```
pip install -r requirements.txt
```
## ▶️ How to Run the Project

1. Make sure Python 3.8–3.12 is installed

2. Install all dependencies:

    ```
        pip install -r requirements.txt
    ```

3. Run the AirCanvas script:

    ```
         python aircanvas.py
    ```

4. Point your hand toward the webcam and start drawing in the air!